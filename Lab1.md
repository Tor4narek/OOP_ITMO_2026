# Лабораторная работа №1  
---

## Цель работы

Изучение основ объектно-ориентированного программирования в языке C#:  
работа с **классами, структурами и перечислениями**, различия между **value types и reference types**,  
использование **конструкторов, свойств, методов, static и const**,  
применение **наследования, абстрактных классов, интерфейсов, полиморфизма и исключений**.


## Выбор варианта

Номер варианта — номер в журнале.  
<br>**[Список вариантов](/lab1_variants)**<br>

Вариант определяет предметную область и набор действий приложения.


## Общая идея приложения

Необходимо реализовать консольное приложение, выполняющее основное действие предметной области  
(например, бронирование номера в отеле).

- Все данные хранятся **в памяти программы**
- Для хранения используются **коллекции**
- Приложение работает в рамках **одного запуска**
- Архитектура должна допускать дальнейшее расширение


## Техническое задание

### 1. Базовые типы данных

**Задача:**  
Определить вспомогательные типы данных предметной области.

#### Требования:

- Реализовать **как минимум одно перечисление (`enum`)**
  (статус, тип, категория и т.п.)
- Реализовать **как минимум одну структуру (`struct`)**, предназначенную для:
  - хранения логически связанных данных
  - передачи по значению
- `struct` должен:
  - иметь конструктор
  - использоваться внутри классов
- В коде должна быть продемонстрирована разница между `struct` и `class`


### 2. Абстрактный уровень архитектуры

**Задача:**  
Определить общие контракты и базовую сущность системы.

#### Требования:

- Реализовать:
  - **как минимум один интерфейс**
  - **один абстрактный класс**
- Интерфейс должен описывать действие предметной области  
  (бронирование, аренда, регистрация и т.п.)
- Абстрактный класс должен:
  - представлять обобщённую сущность предметной области
  - содержать общие свойства и методы
  - содержать абстрактный или виртуальный метод
  - иметь один или несколько конструкторов
  - использовать `this`, `static` и `const`


### 3. Наследование и конкретные реализации

**Задача:**  
Реализовать конкретные классы предметной области.

#### Требования:

- Реализовать **не менее двух классов-наследников** абстрактного класса
- Каждый класс должен:
  - иметь собственный конструктор
  - вызывать `base(...)`
  - переопределять (`override`) методы базового класса
- Поведение классов должно логически отличаться
- Работа с объектами должна выполняться:
  - через базовый класс
  - через интерфейс  
  (демонстрация полиморфизма обязательна)


### 4. Исключения и обработка ошибок

**Задача:**  
Реализовать обработку ошибок предметной логики.

#### Требования:

- Использовать стандартные исключения:
  - `ArgumentException`
  - `InvalidOperationException` (или аналогичные)
- Реализовать **как минимум одно пользовательское исключение**
- Исключения должны выбрасываться при:
  - некорректном вводе данных
  - невозможности выполнения действия  
    (например, попытка забронировать занятый объект)
- В `Main` обязательно использовать `try-catch-finally`


### 5. Консольный сценарий работы

**Задача:**  
Реализовать взаимодействие с пользователем через консоль.

#### Требования:

Приложение должно:

- Отображать меню:
  - выполнить основное действие
  - вывести текущее состояние данных
  - выход
- Получать ввод через `Console.ReadLine`
- Выполнять действие предметной области
- Хранить данные в **коллекциях** (`List<T>` или аналог)
- Корректно обрабатывать ошибки ввода и логики


## Требования к реализации

В программе **обязательно** должно быть использовано:

- `struct`, `enum`
- `class`, `abstract class`
- интерфейсы
- наследование и полиморфизм
- конструкторы
- `this`, `static`, `const`
- `ref`, `out`
- исключения (`try-catch`, `throw`)

### Разрешено:

- Коллекции (`List<T>`)
- Консольный ввод/вывод
- Стандартные библиотеки C#

### Запрещено:

- Работа с файлами
- LINQ
- Базы данных
- Внешние библиотеки
- Сериализация


## Критерии оценки

### Код программы (6 баллов):

- **2 балла** — корректная работа программы
- **2 балла** — логичная и расширяемая архитектура
- **2 балла** — осмысленное использование ООП

### Защита работы (6 баллов):

- **1 балл** — объяснение архитектуры
- **3 балла** — обоснование проектных решений
- **2 балла** — ответы по темам лекций 1 и 2

## Полезные ссылки
https://learn.microsoft.com/ru-ru/dotnet/csharp/fundamentals/tutorials/oop
https://learn.microsoft.com/ru-ru/dotnet/csharp/language-reference/builtin-types/value-types
https://learn.microsoft.com/ru-ru/dotnet/csharp/language-reference/keywords/reference-types
https://learn.microsoft.com/ru-ru/dotnet/csharp/fundamentals/tutorials/inheritance
https://learn.microsoft.com/ru-ru/dotnet/csharp/language-reference/statements/exception-handling-statements#the-try-catch-statement

Также будет плюсом, если вы ознакомитесь с правилами код-стайла и будете придерживаться их при выполнении работы.
https://learn.microsoft.com/ru-ru/dotnet/csharp/fundamentals/coding-style/identifier-names
